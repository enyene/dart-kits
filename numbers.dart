// import math lib
import 'dart:math';

void main() {
  // integer

  int quantity = 300;

  // float

  double price = 50.55;

  print(quantity / price);

  quantity = quantity + 50;
  quantity += 50;
  quantity++;

  print(quantity);
  print(min(quantity, price));
  print(sqrt(0.987));
}
